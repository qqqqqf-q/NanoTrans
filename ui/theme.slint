// NanoTrans Design System
// Blue Palette Theme

export global Theme {
    // Colors
    out property <brush> background-main: #1e1e1e;
    out property <brush> background-overlay: #2d2d2d;
    out property <brush> background-input: #3c3c3c;
    
    out property <brush> text-primary: #cccccc;
    out property <brush> text-secondary: #999999;
    out property <brush> text-placeholder: #666666;
    
    out property <brush> accent-primary: #007acc;
    out property <brush> accent-hover: #0098ff;
    out property <brush> accent-pressed: #005a9e;
    
    out property <brush> border-subtle: #454545;
    out property <brush> border-focus: #007acc;

    out property <brush> danger-surface: #3a1515;
    out property <brush> danger-text: #f14c4c;

    // Layout
    out property <length> padding-small: 8px;
    out property <length> padding-medium: 16px;
    out property <length> padding-large: 24px;
    
    out property <length> radius-small: 6px;
    out property <length> radius-medium: 12px;
    out property <length> radius-large: 16px;

    // Typography
    out property <length> font-size-small: 12px;
    out property <length> font-size-body: 14px;
    out property <length> font-size-header: 18px;
    out property <length> font-size-large: 24px;
}
